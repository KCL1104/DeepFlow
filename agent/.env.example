# DeepFlow Agent Environment Configuration

# ===========================================
# LLM Configuration (OpenAI Compatible API)
# ===========================================
OPENAI_API_KEY=your-api-key
OPENAI_API_BASE=https://api.openai.com/v1
LLM_MODEL=gpt-4-turbo
LLM_TEMPERATURE=0

# ===========================================
# Opik (Comet ML) - Agent Tracing & Evaluation
# ===========================================
OPIK_API_KEY=your-opik-api-key
OPIK_PROJECT_NAME=DeepFlow
OPIK_WORKSPACE=default

# ===========================================
# Upstash Redis (REST API) - Priority Queue
# Get credentials: https://console.upstash.com/
# ===========================================
UPSTASH_REDIS_REST_URL=https://your-redis.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-redis-rest-token

# ===========================================
# Slack Integration (for send_auto_reply tool)
# Create app: https://api.slack.com/apps
# Required scopes: chat:write, channels:history
# ===========================================
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_SIGNING_SECRET=your-slack-signing-secret

# ===========================================
# Priority Scoring Weights
# ===========================================
PRIORITY_WEIGHT_URGENCY=0.4
PRIORITY_WEIGHT_DEADLINE=0.3
PRIORITY_WEIGHT_WAIT_TIME=0.2
PRIORITY_WEIGHT_CONTEXT=0.1

# ===========================================
# User State Thresholds
# ===========================================
# FLOW: Only urgency >= 9 should interrupt
FLOW_STATE_THRESHOLD=9
# SHALLOW: Urgency >= 6 is acceptable
SHALLOW_STATE_THRESHOLD=6

